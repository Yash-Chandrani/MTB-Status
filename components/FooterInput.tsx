import * as React from "react";
import { StyleSheet, View } from "react-native";

const FooterInput = () => {
  return <View style={styles.footerInput} />;
};

const styles = StyleSheet.create({
  footerInput: {
    width: 375,
    height: 82,
    overflow: "hidden",
  },
});

export default FooterInput;
